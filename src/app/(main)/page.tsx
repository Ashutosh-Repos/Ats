// "use client";
// import { useSession } from "next-auth/react";
export default function Home() {
  // const { data: session } = useSession();
  return (
    <div className="w-screen h-screen relative bg-white dark:bg-zinc-950"></div>
  );
}

// backend is nearly completed now focus on ui anf frontend
